git--------------------commands

To know the file
pwd

To make a directory/folder
mkdir git
cd git 


To pull the code 
gti pull --rebase

To push the code
git add .
git comiit -m "pushing the code"
git push -u

